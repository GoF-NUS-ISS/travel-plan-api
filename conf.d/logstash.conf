input
{
    tcp 
    {
      port => 4560
      codec => json_lines
      tags => ["tcp"]
    }
}


#filter {
#  if [tags][json] {
#     json {
#       source => "message"
#     }
#   }
#}


output {
    elasticsearch {
         hosts => ["elasticsearch:9200"]
         index => "%{appName}-%{+YYYY.MM.dd}"
         manage_template => false
         document_type => "appLogs"
   }
  stdout {
    codec => rubydebug
    }
}
